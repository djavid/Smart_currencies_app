package com.djavid.bitcoinrate.contracts.ticker

import com.djavid.bitcoinrate.TICKER_MODULE
import org.kodein.di.Kodein

class TickerFragmentModule {
	val kodein = Kodein.Module(TICKER_MODULE) {
	
	}
}